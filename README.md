# 🛍️ Shop App (Advanced Flutter Showcase)

Продвинутое E-commerce приложение, демонстрирующее современные подходы к разработке на Flutter: Clean Architecture, Offline-first логику и покрытие тестами.

## 🚀 Ключевые особенности (Key Features)

* **Offline-first архитектура:** Приложение полноценно работает без интернета. Действия пользователя (добавление в корзину, изменение количества) сохраняются в локальную очередь и синхронизируются с сервером при восстановлении связи.
* **Supabase Backend:** Реальная база данных (PostgreSQL) с аутентификацией и Realtime обновлениями.
* **Dependency Injection:** Использование `get_it` для слабой связности компонентов и удобного тестирования.
* **Безопасность:** Управление секретами через `.env`.
* **UX:** Optimistic UI, обработка ошибок, Pull-to-Refresh, индикация оффлайн-режима.
* **Unit Testing:** Покрытие критической бизнес-логики корзины модульными тестами (`mocktail`).

## 🛠️ Технический стек

* **Framework:** Flutter
* **State Management:** Provider
* **Backend:** Supabase
* **DI:** GetIt
* **Storage:** Hive / Shared Preferences (для токенов)
* **Testing:** flutter_test, mocktail

## 🧪 Тесты

В проекте реализованы Unit-тесты для `CartProvider`, проверяющие:
* Расчет итоговой суммы (`totalAmount`).
* Инициализацию состояния.
* Логику загрузки данных с мокированием сервисов.

Для запуска тестов:
```bash
flutter test# shop-app
# shop-app
